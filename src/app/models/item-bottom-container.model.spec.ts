import { ItemBottomContainer } from './item-bottom-container.model';

describe('ItemBottomContainer', () => {
  it('should create an instance', () => {
    expect(new ItemBottomContainer()).toBeTruthy();
  });
});
