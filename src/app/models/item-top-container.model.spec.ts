import { ItemTopContainer } from './item-top-container.model';

describe('ItemTopContainer', () => {
  it('should create an instance', () => {
    expect(new ItemTopContainer()).toBeTruthy();
  });
});
