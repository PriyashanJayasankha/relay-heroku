import { ChartTopContainer } from './chart-top-container.model';

describe('ChartTopContainer', () => {
  it('should create an instance', () => {
    expect(new ChartTopContainer()).toBeTruthy();
  });
});
